This models show 3 examples of variations of the one shown in the exercise statement, one is correct and the others contain errors. You're encouraged to change some references to pages on navigator buttons and test if pages are reacheable, for example. Provided validators, quickFixes and page scopes (both for every button --> each button currentPage can only be it's container; returnbutton --> srcPage cannot be currentPage; navigatorButton --> dstPage & alterDstPage cannot be currentPage) are shown through the 2 examples with errors.
